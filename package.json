{
  "name": "base",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "start": "bun run --filter=\"@expo-app/app\" start",
    "clean": "bun run --filter=\"*\" clean && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}