{
  "name": "base",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "clean": "bun run --filter=\"*\" clean && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}